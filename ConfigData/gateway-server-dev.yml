server:
  port: 9020

application:
  location: config (Github)
  config:
    profile: dev
    description: configuration from "gateway-server-dev.yml"

eureka:
  client:
    service-url:
#      defaultZone: http://localhost:8761/eureka
      defaultZone: http://eureka-server:8761/eureka
    enable: false